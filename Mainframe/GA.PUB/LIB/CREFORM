SET EXEC NOLOG TERSE
; THIS EXECFILE IS MAINTAINED BY JOHN SACK AND ROBIN SPRING.
CALL SPIRES(BW)
SELECT FORMATS
USE WYL.GA.PUB.LIBDATA#FORDEF CLEAR PUB
/CHANGE 'ID =       ' TO 'ID = $ACCOUNT.MAIL.LIST;' NOLIST
/CHANGE 'FILE =       ' TO 'FILE = $ACCOUNT.MAIL.LIST;' NOLIST
ADD
COMMENT YOUR FORMAT HAS BEEN ADDED TO THE FORMATS SUBFILE
CALL SPICOMP(W)
FORMAT MAIL.LIST
CLEAR EXEC
