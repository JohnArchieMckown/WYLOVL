.cs 7 on
.item 'SET WVOLUMES Command' 'Added on 10/11/84 by LSO'
.boxon
SET WVOLUMES [volume1[,volume2[,volume3[,volume4[,volume5[,volume6]]]]]]
.boxoff

.* DESCRIPTION
Required Privileges:  SYSTEMS

The SET WVOLUMES command sets the default volumes for non-specific volume
requests.  The SHOW WVOLUMES command displays the current volume list.

.* SELECT 'SET WVOLUMES Command Options'
.us Option Descriptions

.* OPTIONS [volume1[,volume2[,volume3[,volume4[,volume5[,volume6]]]]]]
.ix volume 'on SET WVOLUMES command'
[volume1[,volume2[,volume3[,volume4[,volume5[,volume6]]]]]]
\\\\\\\\\\\\\\\\specifies from 1-6 volumes that WYLBUR should select when a
                non-specific volume request is made for a permanent data set.
                If no volume is specified, the current volume list is removed.
.cs 7 off

