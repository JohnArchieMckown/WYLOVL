.cs 7 on
.item '$SET NORELEASE Command' 'Added on 10/02/82 by LSO'
.boxon
$SET NORELEASE
.boxoff

.* DESCRIPTION
Required Privileges:  OPERATOR or SYSTEMS

The $SET NORELEASE command allows you to stop the automatic release of tape
setup jobs with the following /*SETUP cards:

     INPUT=CATLG     INPUT=CAT
     OUTPUT=CATLG    OUTPUT=CAT
     OUTPUT=SCRTCH   OUTPUT=SCRATCH  OUTPUT=KEEP   OUTPUT=SAVE

The system default is $SET RELEASE.  To display the tape job setup release
option in effect, issue the $SHOW RELEASE command.
.cs 7 off
