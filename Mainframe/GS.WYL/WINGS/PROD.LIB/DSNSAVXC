xproc () begin
declare string vv
declare num counter
use wings.data.small clr
loop:
try sav wings.data.savxc temp rep
counter=counter+1
sv counter

if (counter gt 1000) goto quit

if (tryerr) begin
   sv counter tryid trymsg
   goto quit
   end
goto loop
quit:
end
